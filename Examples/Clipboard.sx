use GFX.Clipboard

func main() {
    if !Clipboard.has_text() {
        print("The system clipboard contains no text.")
        return
    }
    let value = Clipboard.read_text()
    print("Clipboard text length: ", value.count())
    Clipboard.write_text(value)
}
