use GFX.Scene3D.MeshHandle

public struct Mesh {
    var asset:MeshHandle
    var visible:bool

    init(asset:MeshHandle = MeshHandle(), visible:bool = true) {
        self.asset = asset
        self.visible = visible
    }
}
