use GFX.Bootstrap.Plugin
use GFX.Bootstrap.Application
use GFX.Plugins.ECSPlugin

public struct TransformPlugin:Plugin {
    func id() str { return "GFX.Plugins.TransformPlugin" }

    func build(application:Application) {
        application.install(ECSPlugin())
    }
}
